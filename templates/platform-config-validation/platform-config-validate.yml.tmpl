# Optional platform/deploy config schema validation.
#
# Enable by copying this file to:
#   .github/workflows/platform-config-validate.yml
#
# The reusable workflow validates matching YAML files against
# @jorisjonkers-dev/deploy-config-schema. Keep this workflow opt-in for repos that
# actually carry platform/deploy config, and keep the reusable workflow pinned
# to the migration release tag.
name: Platform Config Validate

on:
  pull_request:
    paths:
      - "platform/**/*.yml"
      - "platform/**/*.yaml"
      - "deploy/**/*.yml"
      - "deploy/**/*.yaml"
      - ".github/workflows/platform-config-validate.yml"
  push:
    branches: [main]
    paths:
      - "platform/**/*.yml"
      - "platform/**/*.yaml"
      - "deploy/**/*.yml"
      - "deploy/**/*.yaml"
      - ".github/workflows/platform-config-validate.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  platform-config-validate:
    name: Platform Config Validate
    uses: JorisJonkers-dev/github-workflows/.github/workflows/platform-config-validate.yml@v0.6.0
    with:
      config-paths: |
        platform/**/*.yml
        platform/**/*.yaml
        deploy/**/*.yml
        deploy/**/*.yaml
      schema-kind: auto
