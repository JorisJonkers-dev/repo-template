# Vendored CI actions tree checked out by the shared reusable workflows into .github-workflows/.
# It is CI machinery, not this project's code. Prettier does not read .gitignore, so it is listed
# here explicitly (eslint picks it up from .gitignore / the shared preset).
.github-workflows

# Build output / dependencies
dist
build
node_modules
coverage
